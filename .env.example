# Copy to .env and fill in. Never commit .env.
# Rotate the old WAQI key at https://aqicn.org/data-platform/token/ -- the
# previous one was committed in plaintext and is in git history.
WAQI_TOKEN=
DAGSHUB_TOKEN=
